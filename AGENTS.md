## Learned Workspace Facts

- Python dependencies in this repo are managed with uv: `pyproject.toml` is the source of truth; use `uv sync` or the project `start-uv` scripts for environment setup rather than treating `requirements.txt` as the primary install path when working with uv.
